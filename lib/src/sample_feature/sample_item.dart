/// A placeholder class that represents an entity or model.
class SampleItem {
  /// Creates a [SampleItem] with the given [id].
  const SampleItem(this.id);

  /// The unique identifier for this item.
  final int id;
}
